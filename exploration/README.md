# Manuscript analyses and figures

This directory is intentionally left minimal until the final manuscript panels are frozen. Each retained analysis will have one documented entry point, explicit input files, and outputs below the external `output_root` in `configs/paths.yaml`.
